numpy
pytest
sentence-transformers
fastapi
uvicorn
httpx>=0.27,<1
python-dotenv>=1,<2
python-multipart
pypdf

# Database foundation (PostgreSQL + pgvector). This PR only provisions the
# infrastructure; the default VECTOR_STORE_BACKEND=memory keeps the current
# JSON metadata repository and in-memory NumPy index in charge.
SQLAlchemy>=2.0,<3
alembic>=1.18,<2
psycopg[binary]>=3.3,<4
pgvector>=0.5,<1
